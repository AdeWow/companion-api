# Server
PORT=3000
NODE_ENV=development

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# Redis (will be auto-linked by Railway when you add Redis service)
REDIS_URL=redis://localhost:6379

# Expo Push (for ticket 1.5 — leave empty for now)
EXPO_ACCESS_TOKEN=
